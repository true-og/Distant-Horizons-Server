name: DHSupport
version: ${project.version}
main: ${main.class}
api-version: ${mc.api-version}
mc-version: ${mc.api-version}${mc.api-version-patch}
prefix: DHS
folia-supported: true
author: Jckf
commands:
  dhs:
    description: Manage DH Support.
    usage: /dhs
    permission: distant_horizons.admin
  dh:
    description: Not used.
    usage: /dh
    permission: distant_horizons.admin
